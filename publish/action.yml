name: 'Publish MoonBit Package'
description: 'Publish a MoonBit package to the mooncakes.io registry'

inputs:
  token:
    description: >
      API token for the mooncakes.io registry. Store this as a repository
      secret and pass it with `${{ secrets.MOONCAKES_TOKEN }}`.
    required: true
  package-path:
    description: >
      Path to the package directory that contains moon.pkg.json.
      Defaults to the repository root.
    required: false
    default: '.'

runs:
  using: 'composite'
  steps:
    - name: Save credentials and publish package
      shell: bash
      working-directory: ${{ inputs.package-path }}
      env:
        MOONCAKES_TOKEN: ${{ inputs.token }}
      run: |
        set -euo pipefail
        mkdir -p ~/.moon
        trap 'rm -f ~/.moon/credentials.json' EXIT
        printf '%s' "$MOONCAKES_TOKEN" > ~/.moon/credentials.json
        moon whoami
        moon publish
